base: 'ubuntu/trusty'

apt:
  packages:
    - build-essential
    - git
    - supervisor
    - nginx
    - pypy

npm:

add:
  supervisor.conf: /etc/supervisor/conf.d/pies.conf
  index.html: /usr/share/nginx/html/index.html

after_install: 
  - echo "HELLO! running a custom command"

cmd: ['supervisord', '-n']
