base: 'ubuntu/trusty'

apt:
  packages:
    - build-essential
    - git
    - supervisor
    - nginx

add:
  supervisor.conf: /etc/supervisor/conf.d/pies.conf
  index.html: /usr/share/nginx/html/index.html

cmd: ['supervisord', '-n']
